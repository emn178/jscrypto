{
  "name": "@crypto/monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Composable cryptography components for JavaScript and TypeScript.",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/classic"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b --pretty false",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.2.2",
    "typescript": "^5.5.0"
  },
  "license": "MIT"
}
